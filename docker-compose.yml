services:
  tripodo:
    build: .
    container_name: tripodo
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - app-network

networks:
  app-network:
    external: true
